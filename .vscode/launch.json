{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Warpunk (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/debug/magicians_misfits.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "env": {
                "VK_LOADER_DEBUG": "all"
            },
            "terminal": "integrated"
        }
    ]
}
